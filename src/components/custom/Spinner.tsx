const Spinner = () => {
  return (
    <div className="flex justify-center items-center h-screen w-full">
      <div className="w-10 h-10 border-t-2 border-b-2 border-dotted border-black animate-spin rounded-full"></div>
    </div>
  );
};

export default Spinner;
